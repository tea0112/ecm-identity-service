# ============================================
# ECM Identity Service - Environment Variables
# ============================================
# IMPORTANT: Use environment-specific .env files
# - .env.local  → For localhost development
# - .env.dev    → For shared dev server
# - .env.uat    → For UAT environment
# - .env.prod   → For production
#
# All environments use the SAME variable names
# Copy the appropriate .env.{environment} file for your use case
# ============================================

# ============================================
# Database Configuration
# ============================================
# Same variable names across all environments
DB_URL=jdbc:postgresql://localhost:5432/ecm_identity_dev
DB_USERNAME=dev_ecm
DB_PASSWORD=your_password_here
DB_SCHEMA=dev_ecm

# ============================================
# Spring Profile
# ============================================
# Options: local, dev, uat, prod
SPRING_PROFILES_ACTIVE=local

# ============================================
# Usage Examples:
# ============================================
# Local development:
#   cp .env.local .env
#   export $(cat .env | xargs)
#   ./gradlew bootRun
#
# Shared dev server:
#   cp .env.dev .env
#   export $(cat .env | xargs)
#   ./gradlew update -PrunList=dev
#
# UAT deployment:
#   export $(cat .env.uat | xargs)
#   ./gradlew update -PrunList=uat
#
# Production deployment:
#   export $(cat .env.prod | xargs)
#   ./gradlew update -PrunList=prod
#
# With Docker:
#   docker run --env-file .env.prod app:latest
#
# ============================================
# Security Notes:
# ============================================
# ✅ DO:
#   - Use separate .env files per environment
#   - Keep .env files out of version control
#   - Use secrets management for prod (Vault, AWS Secrets Manager)
#   - Rotate passwords regularly
#
# ❌ DON'T:
#   - Commit any .env file to Git
#   - Share passwords via Slack/email
#   - Use same password across environments
# ============================================
